import { Constraint } from 'draco-core';
import { TopLevelUnitSpec } from 'vega-lite/build/src/spec/unit';
import { Model } from './draco';
/**
 * Get the array of witnesses from clingo output.
 * Return undefined if no witnesses were found.
 */
export declare function getModels(result: any, constraints: Constraint[]): Model[];
export declare function models2vl(models: Model[]): TopLevelUnitSpec[];
